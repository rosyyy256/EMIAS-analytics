using System;
using System.Collections.Generic;
using EMIAS_anal_ytics;
using NUnit.Framework;

namespace EMIAS_Tests
{
    [TestFixture]
    public class Tests
    {
        [Test]
        public void DepPushTest()
        {
            // var actual = new Department
            // {
            //     Name = "dep1"
            // };
            // for (var i = 0; i < 10; i++)
            // {
            //     actual.Push(new Row("1", "123", "doc"));
            // }
            // var expected = new Department
            // {
            //     DoctorsList = new List<Doctor> {new Doctor("doc") {SemdCount = 10}}
            // };
            // var a = new Dictionary<int, int>();
            // a[1] = 11;
            // a[2] = 22;
            //
            // Assert.AreEqual(expected.DoctorsList.Count, actual.DoctorsList.Count);
            // Assert.AreEqual(expected.DoctorsList[0].SemdCount, actual.DoctorsList[0].SemdCount);
            // Assert.AreEqual(a.Count, 2);
            // Assert.AreEqual(a[1] + a[2], 33);
        }
    }
}